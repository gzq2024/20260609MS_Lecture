# Pomodoro Timer 実装機能一覧

## 1. MVPで必須の機能

1. タイマー表示機能
- 作業モード中の残り時間を mm:ss で表示する。

2. 開始機能
- 開始ボタンで作業タイマーを開始できる。

3. リセット機能
- リセットボタンで現在セッションを初期状態に戻せる。

4. 円形プログレス表示
- 残り時間に連動してリング表示が更新される。

5. モード表示
- 現在状態（例: 作業中）を画面に表示できる。

6. セッション完了判定
- 0秒到達時にセッション完了を1回だけ確定する。

7. 自動状態遷移
- 作業完了後に休憩へ、休憩完了後に作業へ遷移する。

8. 長休憩ルール
- 作業4回ごとに長休憩へ切り替える。

9. 今日の進捗表示
- 完了セッション数と当日集中時間を表示する。

10. セッション記録API
- 作業完了時にセッション情報を保存できる。

11. 今日の集計API
- 保存データから当日の件数・合計時間を取得できる。

12. 初期画面配信
- Flaskで画面を配信し、フロントエンドを起動できる。

## 2. テストしやすさのために必要な機能

1. 時刻抽象化
- 現在時刻取得を差し替え可能にする（Clock導入）。

2. 状態遷移ロジックの分離
- UIから独立した純粋関数で遷移判定を行う。

3. スケジューラ抽象化
- タイマー進行処理を差し替え可能にする（Scheduler導入）。

4. 永続化インターフェース化
- 本番実装とテスト用インメモリ実装を切り替え可能にする。

5. APIバリデーション
- 不正入力を拒否し、異常系テストを明確にする。

6. Presenter分離
- 描画処理を render(viewModel) に集約し、DOM依存を減らす。

7. 設定注入
- 作業時間や休憩時間を注入可能にし、短時間設定で高速テストできるようにする。

8. イベントログ
- started, paused, completed などを記録し、遷移過程を検証可能にする。

## 3. 品質要件として必要な機能

1. タブ非アクティブ復帰時の時間整合
- バックグラウンド復帰後も残時間が正しい。

2. リロード時の状態復元
- セッション中に再読み込みしても状態を復元できる。

3. 重複記録防止
- 同一セッション完了が二重保存されない。

4. エラーハンドリング
- API失敗時にUIが壊れず、再試行または通知できる。

5. レスポンシブ対応
- PCとモバイルでレイアウトが崩れない。

## 4. 将来拡張機能（後続フェーズ）

1. 作業時間・休憩時間の設定変更
2. 一時停止・再開ボタン
3. 通知音・ブラウザ通知
4. 週間・月間レポート
5. タスク名とメモ管理
