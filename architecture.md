# Pomodoro Timer Webアプリケーション アーキテクチャ案

## 1. 目的
このドキュメントは、Flask + HTML/CSS/JavaScriptで実装するポモドーロタイマーWebアプリの設計方針を定義する。
主な目的は次の3点。

- モックに沿ったUIを正確に実装する
- タイマー処理の正確性（時間ずれ・重複完了防止）を担保する
- ユニットテストしやすい構造で保守性を高める

## 2. 全体方針

- 単一ページアプリとして開始し、過剰な分散構成は取らない
- Flaskは「画面配信」と「記録/集計API」を担当
- カウントダウン自体はブラウザ側で実行
- 残時間は減算ループではなく「終了予定時刻との差分」で算出

## 3. 論理アーキテクチャ

- Presentation層（Browser）
  - HTML/CSS: UI描画
  - JavaScript: イベント受付と描画更新
- Domain/Application層（Browser JS）
  - タイマー状態機械
  - 残時間計算
  - 状態遷移制御
- Backend層（Flask）
  - テンプレート配信
  - REST API
- Data層
  - 初期: SQLite（またはJSON）
  - 将来: DB拡張可能なRepository設計

## 4. 推奨ディレクトリ構成

```text
/workspaces/20260609MS_Lecture/
  architecture.md
  README.md
  1.pomodoro/
    app.py
    templates/
      index.html
    static/
      css/
        style.css
      js/
        app.js
        timer/
          state_machine.js
          clock.js
          scheduler.js
          presenter.js
          config.js
    domain/
      session_repository.py
      models.py
    tests/
      js/
      python/
```

補足:
- ポモドーロ関連コードは `1.pomodoro/` 配下に集約する
- 今回のドキュメントのみプロジェクトルートに配置する

## 5. 状態管理設計（重要）

### 5.1 状態

- idle
- work
- short_break
- long_break
- paused

### 5.2 主なイベント

- START
- PAUSE
- RESUME
- RESET
- TICK
- COMPLETE

### 5.3 遷移ルール

- idle -> work（開始）
- work -> short_break（作業完了）
- short_break -> work（休憩完了）
- 4セッションごとに short_break の代わりに long_break
- 任意状態で pause/resume/reset を受理

### 5.4 時刻計算

- 開始時: `endAt = now + duration`
- 表示時: `remaining = max(0, endAt - now)`
- 0秒到達時は完了イベントを一度だけ発火（重複防止）

## 6. API設計（最小構成）

- `GET /`
  - 初期画面（テンプレート）を返す
- `POST /api/sessions`
  - 作業セッション完了を記録
  - 入力例: session_type, started_at, ended_at, focus_minutes
- `GET /api/stats/today`
  - 当日の完了件数、集中時間を返す

将来拡張:
- 設定保存API（作業/休憩分数）
- 週間/月間統計API
- ユーザー認証

## 7. データモデル案

`session`（単表で開始）

- id
- session_type（work / short_break / long_break）
- started_at
- ended_at
- focus_minutes
- created_at

日次集計はクエリで算出し、必要になるまで集計専用テーブルは作らない。

## 8. ユニットテスト容易性のための改善ポイント

### 8.1 時刻依存の分離

- `Clock` インターフェースを導入
- 本番: `SystemClock`
- テスト: `FakeClock`

効果:
- 時刻固定テストが可能になり、残時間/完了判定の再現性が上がる

### 8.2 状態遷移の純粋関数化

- `nextState(currentState, event, now, config)` を副作用なしで実装

効果:
- テーブル駆動テストで遷移網羅が容易

### 8.3 スケジューラ抽象化

- `setInterval` 直利用を避け、`Scheduler` 経由に統一

効果:
- 非同期待ちのない安定テスト

### 8.4 永続化の抽象化

- `SessionRepository` をインターフェース化
- 本番: SQLite実装
- テスト: InMemory実装

効果:
- API/サービステストが高速化し、外部依存を減らせる

### 8.5 入出力境界の明確化

- APIリクエスト/レスポンスをスキーマで検証

効果:
- 境界値・異常系テストが明確になる

### 8.6 Presenter分離

- DOM直操作を `render(viewModel)` に集約

効果:
- ロジックをDOM非依存でテスト可能

### 8.7 設定注入

- 作業時間/休憩時間を `config` 注入

効果:
- 短時間設定で高速テスト、長休憩分岐も検証しやすい

### 8.8 イベントログ

- `started` `paused` `completed` などのイベントを記録

効果:
- 最終状態だけでなく遷移過程を検証可能

## 9. テスト戦略

- Unitテスト（最優先）
  - 状態遷移
  - 残時間計算
  - 長休憩切替
  - 0秒境界・重複完了防止
- APIテスト
  - `POST /api/sessions` 正常/異常
  - `GET /api/stats/today` 集計整合
- 最小E2E
  - 開始 -> 完了 -> 休憩
  - リロード復元
  - モバイル表示確認

推奨比率: Unit多め / API中程度 / E2E少なめ

## 10. 実装優先順位

1. 状態機械 + Clock抽象 + Scheduler抽象
2. 基本UI（モック再現）とPresenter
3. Flask API（記録/当日集計）
4. 永続化Repository実装
5. テスト拡充（Unit -> API -> E2E）

この順序で進めることで、初期段階から品質と実装速度を両立しやすい。

## 11. 優先度付き実装バックログ（MVP 1週分）

このセクションは、そのままIssue化できる粒度で記載する。
優先度は P0（必須）/ P1（重要）/ P2（余力対応）とする。

### 11.1 チケット一覧

| ID | タイトル | 優先度 | 目安工数 | 依存 |
|---|---|---|---|---|
| PMD-001 | Flask初期画面配信とディレクトリ整備 | P0 | 0.5日 | なし |
| PMD-002 | モック準拠の基本UI実装（HTML/CSS） | P0 | 1.0日 | PMD-001 |
| PMD-003 | タイマー状態機械（純粋関数）実装 | P0 | 1.0日 | PMD-001 |
| PMD-004 | Clock/Scheduler抽象とカウントダウン実装 | P0 | 0.75日 | PMD-003 |
| PMD-005 | PresenterとUIイベント接続（開始/リセット） | P0 | 0.75日 | PMD-002, PMD-004 |
| PMD-006 | セッション完了記録API（POST /api/sessions） | P0 | 0.5日 | PMD-001 |
| PMD-007 | 当日集計API（GET /api/stats/today） | P0 | 0.5日 | PMD-006 |
| PMD-008 | フロントの進捗カード連携（完了数/集中時間） | P0 | 0.5日 | PMD-005, PMD-007 |
| PMD-009 | JSユニットテスト（状態遷移/時刻計算） | P0 | 0.75日 | PMD-004 |
| PMD-010 | Flask APIテスト（正常/異常/境界） | P0 | 0.5日 | PMD-006, PMD-007 |
| PMD-011 | リロード復元と重複完了防止 | P1 | 0.5日 | PMD-005 |
| PMD-012 | モバイル表示最適化と最小E2E確認 | P1 | 0.5日 | PMD-008 |

合計目安: 約7.75人日（1人で1週間想定、バッファ込みで調整可能）

### 11.2 チケット詳細

#### PMD-001 Flask初期画面配信とディレクトリ整備

- 目的: Flaskでトップ画面を返し、以降の実装土台を確立する
- 作業内容:
  - `1.pomodoro/app.py` に `GET /` を実装
  - `templates/` `static/css/` `static/js/` を作成
  - 開発起動手順をREADMEに追記
- 受け入れ条件:
  - ブラウザでトップ画面が表示される
  - 静的ファイルが配信される

#### PMD-002 モック準拠の基本UI実装（HTML/CSS）

- 目的: 添付モックに沿った見た目を再現する
- 作業内容:
  - ヘッダー、モード表示、円形タイマー、ボタン、進捗カードを実装
  - デスクトップとモバイル幅でレイアウト崩れを解消
- 受け入れ条件:
  - モックの主要要素が揃っている
  - 主要解像度で重なり崩れがない

#### PMD-003 タイマー状態機械（純粋関数）実装

- 目的: UI非依存で状態遷移ロジックを確立する
- 作業内容:
  - 状態: idle/work/short_break/long_break/paused
  - イベント: START/PAUSE/RESUME/RESET/TICK/COMPLETE
  - 4セッションごとの長休憩分岐を実装
- 受け入れ条件:
  - 状態遷移関数が副作用を持たない
  - 主要遷移が仕様通り

#### PMD-004 Clock/Scheduler抽象とカウントダウン実装

- 目的: 時刻依存と非同期依存を分離し、テスト性を担保する
- 作業内容:
  - `Clock`（System/Fake）を実装
  - `Scheduler`（本番/テスト）を実装
  - `endAt - now` 方式で残時間計算
- 受け入れ条件:
  - スリープ復帰やタブ非アクティブ後も残時間整合
  - テストで任意時刻を再現可能

#### PMD-005 PresenterとUIイベント接続（開始/リセット）

- 目的: ロジックとDOM操作の責務を分離する
- 作業内容:
  - `render(viewModel)` 方式で描画集約
  - 開始/リセットをイベント接続
  - 円形プログレスと残時間文字列を同期
- 受け入れ条件:
  - 開始でカウントダウン、リセットで初期値へ戻る
  - 状態に応じて表示が一貫する

#### PMD-006 セッション完了記録API（POST /api/sessions）

- 目的: 作業完了をサーバーに記録する
- 作業内容:
  - 保存先（SQLiteまたはJSON）を実装
  - 入力バリデーションを追加
  - 重複完了保存を防止
- 受け入れ条件:
  - 正常データで保存できる
  - 不正入力時に適切なエラーを返す

#### PMD-007 当日集計API（GET /api/stats/today）

- 目的: 進捗カード表示に必要な当日集計を提供する
- 作業内容:
  - 当日完了件数・集中時間を返却
  - 日付境界を考慮した集計処理
- 受け入れ条件:
  - テストデータに対し期待集計値を返す

#### PMD-008 フロントの進捗カード連携（完了数/集中時間）

- 目的: UIとAPIを接続し、進捗を可視化する
- 作業内容:
  - 初期表示時に当日集計取得
  - セッション完了後に再取得して更新
  - API失敗時のフォールバック表示
- 受け入れ条件:
  - 完了数/集中時間が画面上で更新される
  - API失敗時も画面が壊れない

#### PMD-009 JSユニットテスト（状態遷移/時刻計算）

- 目的: 破壊されやすいロジックを先に固定する
- 作業内容:
  - 状態遷移テーブルテスト
  - 0秒境界と重複完了防止
  - 4セッション長休憩切替
- 受け入れ条件:
  - 主要ケースが自動テストで通る

#### PMD-010 Flask APIテスト（正常/異常/境界）

- 目的: API品質を継続的に担保する
- 作業内容:
  - `/api/sessions` と `/api/stats/today` を対象にテスト
  - 必須項目欠落、形式不正、境界値を検証
- 受け入れ条件:
  - 正常/異常系の期待ステータス・レスポンスを満たす

#### PMD-011 リロード復元と重複完了防止

- 目的: 実運用時の体験劣化を防ぐ
- 作業内容:
  - localStorageで状態復元
  - 0秒到達時のイベント二重発火防止
- 受け入れ条件:
  - リロード後もセッションが継続表示される
  - 同一完了イベントが重複保存されない

#### PMD-012 モバイル表示最適化と最小E2E確認

- 目的: 最低限の表示品質と動作品質を担保する
- 作業内容:
  - モバイル向け余白・文字サイズ・ボタン領域調整
  - 開始→完了→休憩遷移の手動E2E確認
- 受け入れ条件:
  - 主要スマホ幅で操作性を満たす
  - E2Eチェックリストを完了

### 11.3 1週間の実行計画（例）

- Day 1: PMD-001, PMD-002
- Day 2: PMD-003, PMD-004
- Day 3: PMD-005, PMD-006
- Day 4: PMD-007, PMD-008
- Day 5: PMD-009, PMD-010, PMD-011, PMD-012（調整込み）

### 11.4 チケットテンプレート

以下のテンプレートを各IDに流用する。

```md
## 概要
- ID:
- タイトル:
- 優先度:
- 目安工数:
- 依存チケット:

## 目的

## 実装内容
-

## 受け入れ条件
-

## テスト観点
-
```
